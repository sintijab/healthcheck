{
  "name": "healthcheck",
  "version": "1.0.0",
  "description": "This project contains a workflow for health check for varienty of web services.",
  "main": "health-check.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "health-check": "node scripts/health-check.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "playwright": "^1.51.0"
  }
}
